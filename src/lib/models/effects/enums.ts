export enum Targets {
    user,
    opponent,
    all,
}

export enum Categories {
    piercing,
	slashing,
	blunt,
	fire,
	ice,
	lightning,
	poison,
	dark,
	holy,
	earth,
	wind,
	water,
	light,
	shadow,
	neutral
}

export enum Types {
    physical,
    magical
}

export enum Ranges {
    none = -1,
    close = 0,
    medium = 1,
    far = 2
}